#pragma once
#include <vector>

class Solution {
public:
	std::vector<int> twoSum(std::vector<int>, int target);
	std::vector<int> twoSum_hash(std::vector<int>, int target);//hash map version of twosum
};